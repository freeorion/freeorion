#include <util/Version.h>

namespace {
    static const std::string retval = "v${FreeOrion_VERSION} [Rev ${FreeOrion_WC_REVISION}] ${FreeOrion_BUILDSYS}";
}

const std::string& FreeOrionVersionString()
{
    return retval;
}
